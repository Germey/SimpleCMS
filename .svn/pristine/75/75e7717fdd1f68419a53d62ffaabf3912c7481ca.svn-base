<?php

namespace Home\Controller;

class IndexController extends HomeController {

	//系统首页
    public function index(){
        $this->index = 'index';
        $this->display();
    }

    // public function temp() {
    //     $content_ids = D("TagMapping")->getField('object_id',true);
    //     $f['id'] = array('in', $content_ids);
    //     $map = D("Content")->where()->getField('id, category_id',true);

    //     foreach ($map as $object_id => $object_type) {
    //         $u['object_type'] = $object_type;
    //         $ff['object_id'] = $object_id;

    //         D("TagMapping")->where($ff)->save($u);

    //         var_dump(D("TagMapping")->getLastSql());
    //         break;
    //     }
    // }
}